type Movie {
  id: Int!
  name: String
  score: Int!
}

type Query {
  movies: [Movie]!
  movie(id: Int!): Movie # Movie에서 특정 id에 해당하는 것만 출력
}

type Mutation {
  addMovie(name: String!, score: Int!): Movie!
}
